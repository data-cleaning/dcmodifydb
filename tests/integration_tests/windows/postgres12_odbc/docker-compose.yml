version: '3.7'


volumes:
  pgdata:




services:
    db_windows_postgres12_odbc:
      container_name: "db_windows_postgres12_odbc"
      image: postgres:12
      command: postgres -c listen_addresses='*'
      volumes:
        - type: "bind"
          source: "database"
          target: /pgdata
      environment:
        POSTGRES_DB: "db_windows_postgres12_odbc"
        POSTGRES_USER: "admin"
        POSTGRES_PASSWORD: "admin"
      ports:
        - "5433:5433"

    windows_postgres12_odbc:
      tty: true
      container_name: windows_postgres12_odbc
      command: bash run_tests.sh
      build: 
        context: ../../../../
        dockerfile: tests/integration_tests/windows/postgres12_odbc/dcmodifyDB/Dockerfile
      environment:
        POSTGRES_DB: "windows_postgres12_odbc"
        POSTGRES_USER: "admin"
        POSTGRES_PASSWORD: "admin"
        POSTGRES_HOST: "postgres12_odbc"     
      depends_on:
        - "db_windows_postgres12_odbc"




networks: {mynetwork: {}}