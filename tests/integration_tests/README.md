# Integration tests

This directory contains the integration_tests for DCmodifyDB, to uncover how DCmodifyDB works with multiple database backends. The only currently working integration test is postgres12_odbc.

run:
```
docker compose up
```
In this directory to start all tests on this setup.