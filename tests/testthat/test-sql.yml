rules:
- expr: if (x > 1) x <- 1
  name: M1
  label: 'simple rule'
  description: |
    Description of this modification rule.
    Can span multiple lines.
- expr: if (x > 1) {x <- 1; y <- 2}
  name: M2
  label: 'multiple rules'
  description: |
    Description of this modification rule.
